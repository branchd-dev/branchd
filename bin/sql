#!/bin/bash

if [ $# -eq 0 ]; then
    echo "Usage: $0 \"SQL_QUERY\""
    echo "Example: $0 \"select * from instances;\""
    exit 1
fi

sqlite3 branchd.sqlite -cmd ".mode table" -cmd ".headers on" "$1"
